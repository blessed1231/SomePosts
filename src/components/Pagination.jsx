import React from 'react';

const Pagination = () => {
    function classList(count) {
        
    }



    return (
       <ul>
           elo
           {classList(10)}
       </ul>
    );
};

export default Pagination;